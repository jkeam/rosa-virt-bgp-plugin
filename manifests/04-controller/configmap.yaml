---
# Controller configuration for demo
apiVersion: v1
kind: ConfigMap
metadata:
  name: bgp-vm-controller-config
  namespace: rosa-virt-bgp-system
data:
  config.yaml: |
    # Network configuration
    networkName: "bgp-network"
    networkAttachmentDefinition: "vm-bgp-net"
    cudnSubnet: "192.168.100.0/24"

    # FRR configuration
    frrNamespace: "frr-k8s-system"
    bgpASN: 65100

    # Controller behavior
    reconcileInterval: "30s"
    ipDiscoveryTimeout: "5m"

    # Feature flags
    enableMetrics: true
    enableStatusUpdates: true
